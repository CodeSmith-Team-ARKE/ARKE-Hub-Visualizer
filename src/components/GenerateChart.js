import React, { Component } from 'react';

export default class GenerateChart extends Component {
    render () {
    
    return(
    console.log('this will be the page where user chooses line or bar chart')
    )
    }
}